# BalloonDB V03G0 role map usage

This folder documents how current BalloonOperator PowerShell scripts map into future BalloonDB/BQL roles.

V03G0 only loads and validates the role map. It does not rewrite the scripts.

Current scripts remain adapters/clients.
BalloonDB core becomes the future in-database query engine.
