# Migration from .bpack

`.bpack`/JSONL files are transitional human-readable audit/migration sources.

They are not the final runtime core.

Large packs are not committed to Git. Keep local or release artifacts only.
